apiVersion: apps/v1
kind: Deployment
metadata:
  name: giftcard-api
  namespace: giftcard-sentiment
spec:
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 1
              memory: 768Mi
